import React from 'react';
import PropTypes from 'prop-types';

export default function Home(props) {
    return (
        <div>
            HELLO FROM HOME
        </div>
    );
}

Home.propTypes = {};
