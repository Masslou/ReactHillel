import React from 'react';
import PropTypes from 'prop-types';

export default function Students(props) {
    return (
        <div>
            HELLO FROM STUDENTS
        </div>
    );
}

Students.propTypes = {};
