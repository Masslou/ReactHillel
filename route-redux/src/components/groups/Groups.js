import React from 'react';
import PropTypes from 'prop-types';

export default function Groups(props) {
    return (
        <div>
            HELLO FROM GROUPS
        </div>
    );
}

Groups.propTypes = {};
